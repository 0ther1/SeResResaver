<Page x:Class="SeResResaver.Views.Pages.ResavePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SeResResaver"
      xmlns:res="clr-namespace:SeResResaver.Resources"
      xmlns:c="clr-namespace:SeResResaver.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Page.Resources>
        <c:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Padding="5" Text="{x:Static res:Strings.ResavePage_Help}" TextWrapping="Wrap"/>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" VerticalAlignment="Center" Content="{x:Static res:Strings.ResavePage_GameTitle}"/>

            <ComboBox Grid.Column="1" VerticalAlignment="Center" Padding="10,3" Margin="5,0" SelectedIndex="{Binding GameTitleIndex}" IsEnabled="{Binding IsWorking, Converter={StaticResource InverseBooleanConverter}}">
                <ComboBoxItem Content="Serious Sam 2"/>
                <ComboBoxItem Content="Serious Sam HD"/>
                <ComboBoxItem Content="Serious Sam 3"/>
                <ComboBoxItem Content="Serious Sam Fusion 2017"/>
                <ComboBoxItem Content="Serious Sam 4"/>
            </ComboBox>
        </Grid>
            
        <Label Grid.Row="2" Padding="5" Content="{Binding Status}"/>

        <Grid Grid.Row="3" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ProgressBar Grid.Column="0" Height="20" VerticalAlignment="Center" Maximum="{Binding MaxProgress}" Value="{Binding Progress}"/>

            <Button Content="{Binding ButtonText}" Grid.Column="1" MinWidth="70" Padding="10,3" Margin="5,0" VerticalAlignment="Center" Command="{Binding StartStopRenameCommand}"/>
        </Grid>

        <DataGrid Grid.Row="4" ItemsSource="{Binding Errors}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" CanUserResizeRows="False" HorizontalGridLinesBrush="#FF717171" VerticalGridLinesBrush="#FF717171">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static res:Strings.ResavePage_File}" Binding="{Binding File}"/>
                <DataGridTextColumn Header="{x:Static res:Strings.ResavePage_ErrorMessage}" Binding="{Binding ErrorMessage}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button MinWidth="70" Margin="5" Padding="10,3" Click="OnBackClick" Content="{x:Static res:Strings.Common_Back}" IsEnabled="{Binding CanGoBack}"/>
            <Button MinWidth="70" Margin="0,5" Padding="10,3" Click="OnBackToStartClick" Content="{x:Static res:Strings.ResavePage_BackToStart}" IsEnabled="{Binding IsFinished}"/>
        </StackPanel>
    </Grid>
</Page>
