<Window x:Class="SeResResaver.Views.Dialogs.ParseReferencesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:res="clr-namespace:SeResResaver.Resources"
        xmlns:local="clr-namespace:SeResResaver"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.ParseReferencesDialog_Title}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height"
        Width="400"
        Closing="Window_Closing">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Text="{x:Static res:Strings.ParseReferencesDialog_Help}" Padding="5"/>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="20"/>

        <Button Grid.Row="2" Content="{x:Static res:Strings.Common_Cancel}" MinWidth="70" Padding="10,3" Margin="5" HorizontalAlignment="Right" IsCancel="True"/>
    </Grid>
</Window>
